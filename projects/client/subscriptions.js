/**
 * Created by Andrew on 26/05/2015.
 */

// The client gets only the records and fields defined by the server publish

// Subscribe to the 'projects' recordset that is published by the server
// Provides all projects owned by the user or shared to them by another user
//Meteor.subscribe("projects");

// Subscribe to a recordset that provides a single project (which must be owned by the user or shared to him/her)
//Meteor.subscribe("project", id);
